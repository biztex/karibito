<div id="main">
    <!-- タイトル -->
    @include('product.parts.preview.title')

    <!-- 画像 -->
    @include('product.parts.preview.update-slider')

    <!-- サービス内容 -->
    @include('product.parts.preview.content')

    <!-- 追加オプション -->
    @include('product.parts.preview.option')

    <!-- よくある質問 -->
    @include('product.parts.preview.faq')
</div>